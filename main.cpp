#include <iostream>
#include "litteraux.h"
#include "pile.h"

using namespace std;

int main()
{
    /*
    try
    {
        Forme_decimale dec1 = 1.4, dec2 = 0.4;
        Litteral_numerique *L = new Litteral_numerique(dec1,dec2);
        Litteral_numerique L2 = *L;
        delete(L);
        Pile P;
        P.push(L2);
        P.push(L2 / Litteral_numerique(0.8,0.8));
        P.push(L2*Litteral_numerique(2));
        Pile P2(P);
        cout << P.pop();
        cout  << endl << P2.pop();
        cout  << endl << P.pop();
        cout  << endl << P2.pop();
        cout  << endl << P2.pop();
        cout  << endl << P.pop();
    }
    catch(Exception e)
    {
        e.afficher();
    }

    */

    Pile P;

    return 0;
}
